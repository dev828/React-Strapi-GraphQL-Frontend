import React from 'react';
import Header from './../../components/Header';

const Home = (props) => {
    return (
        <div>
            <Header />
            <div>Home Page</div>
        </div>
    );
};

export default Home;